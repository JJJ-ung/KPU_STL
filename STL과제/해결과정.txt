0. 읽어오기 방법
- string이 자꾸 안읽어와졌다
- 버전의 문제였다
- 자꾸 빈 Player 한개가 추가가 되었다
- Check 함수를 구현하여 비어있으면 리스트에 안담았다

1. 마지막
- back() 으로 마지막 원소 접근했다

2. 평균
- int가 담을 수 있는 최댓값을 넘어서는 수를 다뤄야했다
- 그래서 가장 큰 자료형인 long long을 사용했다

3.
4.

* 그 외 고민한 내용
- Player 객체를 리스트에 담으니 소멸자에서 문자열 배열이 계속 삭제되었다. 그래서 플레이어 객체를 동적할당하고, 리스트에는 그 포인터를 담게 하였다.